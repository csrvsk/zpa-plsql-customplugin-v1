/*
package com.company.plsql;

import org.sonar.plsqlopen.checks.verifier.PlSqlCheckVerifier;
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import org.sonar.plugins.plsqlopen.api.checks.PlSqlCheck;
import java.util.logging.Logger;
import static org.junit.Assert.fail;

public class ViewsJSRJSDCheckTest {

    private static final Logger LOGGER = Logger.getLogger(ViewsJSRJSDCheckTest.class.getName());

    @Test
    public void testViewsJSRJSDCheckOnSqlFiles() {
        String testFilePath = "src/test/resources/ViewsJSRJSDCheck.sql"; // adjust the path if needed
        ViewsJSRJSDCheck check = new ViewsJSRJSDCheck();
        try {
            PlSqlCheckVerifier.Companion.verify(testFilePath, check);
        } catch (AssertionError e) {
            e.printStackTrace();
            LOGGER.info("Assertion error: " + e.getMessage());
            fail(e.getMessage());
        }
    }
}
*/
